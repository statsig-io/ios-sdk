# .gitignore for StatsigSamplesPods

# Generated by `pod install`
StatsigSamples.xcworkspace

# We ignore Podfile.lock because the version and checksum would quickly become stale
Podfile.lock

# SPM clones the full repo, so we ignore the Pods folder to avoid bloat
Pods
